[package]
name = "beacon-protocol"
version = "0.1.0"
description = "Family Beacon's application protocol: envelope codec, message types, consent state machine, transparency ledger"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
# Integration tests see only the crate and its dev-dependencies, and the vector
# suite reads the corpus itself.
serde_json = { workspace = true }

[lints]
workspace = true
